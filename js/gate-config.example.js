/**
 * Шаблон настроек входа (статический HTML/JS).
 *
 * Как использовать:
 * 1. Скопируйте этот файл: js/gate-config.example.js → js/gate-config.js
 * 2. Замените password на свой длинный пароль.
 * 3. Файл js/gate-config.js в .gitignore — не публикуйте его в открытом репозитории с реальным паролем.
 *
 * Ограничения безопасности (важно для «дропа»):
 * - Пароль и логика видны любому, кто скачал страницу: это не серверная авторизация.
 * - Проверка «закрытости» кабинета держится на sessionStorage; в консоли можно выставить флаг вручную.
 * - Для платного контента с реальной защитой нужен бэкенд (логин, подписанные сессии, выдача материалов с сервера).
 */
window.__CRYPTO_INSIDERS_GATE__ = {
  /** Пароль для формы на index.html */
  password: "ЗАМЕНИТЕ_НА_СВОЙ_ДЛИННЫЙ_ПАРОЛЬ",
  /** Ключ sessionStorage после успешного входа (должен совпадать на всех страницах проекта) */
  sessionKey: "site_landing_unlocked",
};
